# App
APP_NAME=SabiTax
DEBUG=true
API_V1_PREFIX=/api/v1

# Database (NeonDB PostgreSQL)
# Replace with your NeonDB connection string (postgres://, postgresql://, or neondb://)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# JWT Authentication
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# AI Assistant (NexusBert)
# To use the tax assistant, configure the NexusBert API URL and output API key (optional)
NEXUSBERT_API_URL=https://nexusbert-sabitax.hf.space
NEXUSBERT_API_KEY=

# Bank Integration (Optional)
MONO_SECRET_KEY=your_mono_secret_key_here
OKRA_SECRET_KEY=your_okra_secret_key_here

# CORS Origins (comma-separated for multiple, or * for all)
CORS_ORIGINS=*
